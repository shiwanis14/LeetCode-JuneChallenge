class Solution {
    public void reverseString(char[] s) {
        int i = 0;
        while(i < s.length/2) {
            char temp = s[i];
            int j = s.length - 1 - i;
            s[i] = s[j];
            s[j] = temp;
            i++;
        }
    }
}
